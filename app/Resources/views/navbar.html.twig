<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Reporting BOSCP</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                {% if app.user %}
                    <li><a href="{{ path('demandes_traitees_veille') }}">Tableau de Bord</a></li>
                    {#<li><a href="{{ path('fos_user_profile_show') }}">Profile</a></li>#}
                    <li><a href=" {{ path('demande_index') }}">Liste des Demandes</a></li>
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('moderateur_partenaire_index') }}">Gestion des Modérateurs</a></li>
                            <li><a href="{{ path('partenaire_index') }}">Gestion des Partenaires</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('user_list') }}">Gestion des Utilisateurs</a></li>
                        </ul>
                    </li>
                    {% endif %}
                    <li><a href="{{ path('fos_user_security_logout') }}"><span class="glyphicon glyphicon-off"></span></a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>